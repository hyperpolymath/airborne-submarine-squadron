# SPDX-License-Identifier: AGPL-3.0-or-later
# Build artefacts
/build/
*.wasm
!dist/airborne-submarine-squadron.wasm

# Backups
.backup/

# Crash logs written by the Deno dev server crash-report endpoint
# Track the directory via logs/.gitkeep but exclude the JSON dumps
logs/*.json

# Test coverage output
cov/

# Diagnostic pages (dev-only)
gossamer/test_splash.html

# IDE / AI agent configs (per-user, not tracked)
.claude/
.q/
.junie/
.clinerules
.cursorrules
.windsurfrules
.github/copilot-instructions.md
target/
node_modules/
_build/
deps/
.elixir_ls/
.cache/
*.log
*.tmp
.DS_Store
Thumbs.db
build/
dist/
