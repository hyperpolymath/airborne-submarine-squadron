// SPDX-License-Identifier: AGPL-3.0-or-later
= Airborne Submarine Squadron Roadmap

== Current Stage

The Gossamer edition now boots a loader, shows retro battle art, draws a live solar-map HUD, and connects the digital instruments with the orbital menu. The system is deterministic, network-ready, and already records leaderboard runs plus damage diagrams in real time.

== Milestones

=== v2.0 — Orbital Gossamer (released)
* [x] Gossamer renderer with KITT-style HUD and digital speedometer/accelerometer.
* [x] Depth charges, torpedoes, missiles, and afterburner toggles.
* [x] Orbit menu with warp hotkeys (F + Enter) and direct 1‑5 destinations, sun hazard burnout, and cave messages.
* [x] Leaderboard persistence, compact legend, and pause settings with sub-skin tuning.

=== v2.1 — Customization Layer (shipped 2026-04-16)
* [x] Colour swatch preview + R/B/P skin-preset hotkeys in the pause menu.
      Spectrum hue still tunable via `,` / `.`, and a live 5-part swatch
      (hull/wings/tower/nose/porthole) renders in the settings panel.
* [x] Damage map refinement: depleted parts flicker/blur in the diagram,
      unused penalty functions (back damage, front control, hull buoyancy)
      are wired into movement, and hull damage bleeds buoyancy underwater.
* [x] Warp preview: solar minimap now shows destination name, hazard
      warnings, live range, and the orbit menu lists all warp hotkeys
      with per-planet accent/sky/enemy colour swatches.
* [x] Pride/Rainbow/Spectrum skins documented in README controls section
      with persistence details and keybind cheatsheet.
* [x] Controls/keybinds extracted into `gossamer/controls.js` — single
      source of truth for key state, DEFAULT_KEYBINDS, rebind actions,
      and the control scheme legend.

=== v3.0 — Multi-platform Deployment (groundwork landed 2026-04-16)
* [x] Linux packaging manifests: `packaging/debian/` (control + rules +
      launcher stub) and `packaging/rpm/airborne-submarine-squadron.spec`.
      Build-ready skeletons; tagged release still needed to produce
      downloadable `.deb` / `.rpm` artifacts.
* [x] Windows installer: `desktop/install.ps1` creates Start Menu +
      Desktop shortcuts with auto-detection of Deno or Node runtime.
* [x] AffineScript build-chain CI gate: `.github/workflows/build-chain.yml`
      enforces that the prebuilt-WASM fallback keeps working, validates
      WASM magic bytes, and checks `build.sh` still documents the
      `affinescript not found` path. Compile notes captured in `BUILD.md`.
* [x] Astrocompass HUD for orbit mode (bow-relative bearing to nearest
      body + warp destination, dashed bearing line, autopilot tag,
      pulsing Sun warning for star destinations).
* [ ] Kinoite/Silverblue Flatpak manifest.
* [ ] macOS `.pkg` bundle.
* [ ] Android/iOS webview wrappers.
* [ ] Minix + RISC-V + Lua/Node wrapper packaging.
* [ ] Tag + release `.deb` / `.rpm` / `.msi` artifacts from CI.

== Work Queue

* Island platformer mode for v2 milestone (Trionic SubCommando).
* Radar/astrocompass: astrocompass shipped; consider extending the
  dial with a mini-radar for hostile bodies / tourist ships.
* Finish the submarine customization screen polish — bind mouse to
  the pause-menu swatches so players can click-to-pick a colour.
* Harden the warp menu instructions (spaceship leaps, afterburner =>
  warp replacement in vacuum, impulse limits, automated `88 MPH` gating).
* Update documentation set (README.adoc, explainme.adoc, ROADMAP.adoc)
  to reflect npm package publishing capabilities.
* Add package.json maintenance to build process and CI/CD pipeline.

== Future Directions

* Orbital mechanics refinement (zoomed maps, solar orbit snare hazards, planet-specific enemy palettes).
* Multiplayer sync, cross-sub coordination, and telemetry sharing between consoles.
* Expand the documentation to include `burble`, `groove`, and `proven` integrations once the core loop stabilizes.

